-- ============================================================
-- إنشاء حساب المدير الابتدائي
-- جمعية سفراء التعليمية
-- ============================================================
-- ملاحظة: هذا الملف مرجعي.
-- يجب إنشاء المستخدم يدوياً عبر Supabase Dashboard > Authentication > Add User
-- أو عبر API باستخدام service_role key.
-- 
-- بعد إنشاء المستخدم، قم بتحديث دوره إلى 'admin' عبر:
-- UPDATE public.users SET role = 'admin' WHERE email = 'admin@sufara.org';
-- ============================================================

-- بيانات المدير الموصى بها:
-- البريد الإلكتروني: admin@sufara.org
-- كلمة المرور: (سيتم تعيينها في Supabase Dashboard)
-- الاسم: مدير النظام
-- الدور: admin
